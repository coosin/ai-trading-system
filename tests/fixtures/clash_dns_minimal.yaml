# Minimal Clash DNS fragment for unit tests (not a full runnable config)
mode: Rule
dns:
  enable: true
  enhanced-mode: fake-ip
  fake-ip-range: 198.18.0.1/16
  fake-ip-filter:
    - "+.okx.com"
    - "+.okex.com"
    - "+.lan"
